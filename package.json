{
  "devDependencies": {
    "autoprefixer": "^10.4.20",
    "postcss-cli": "^11.0.0"
  },
  "scripts": {
    "build:sass": "sass main.scss main.css",
    "build:css": "postcss main.css --use autoprefixer -o main.prefixed.css",
    "build": "npm run build:sass && npm run build:css"
  },
  "browserslist": [
    "last 2 versions",
    "not dead",
    "IE 11"
  ]
}
